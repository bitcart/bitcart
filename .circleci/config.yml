version: 2.1

executors:
  main-executor:
    parameters:
      v:
        type: string
    docker:
      - image: cimg/python:<< parameters.v >>
      - image: circleci/redis:latest
      - image: circleci/postgres:9-alpine-ram
        environment:
          POSTGRES_DB: bitcart_test
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: 123@

commands:
  install_dependencies:
    parameters:
      v:
        type: string
        default: "3.7"

    steps:
      - restore_cache:
          keys:
            - v32-dependencies-{{ checksum "requirements/web.txt" }}-{{ checksum "requirements/test.txt" }}-{{ checksum "requirements/daemons/btc.txt" }}-{{ .Branch }}-<< parameters.v >>

      - run:
          name: install dependencies
          command: |
            python3 -m venv ~/venv
            . ~/venv/bin/activate
            sudo apt update
            sudo apt install libsecp256k1-dev
            pip install -U -r requirements.txt
            pip install -U -r requirements/daemons/btc.txt
            pip install -U -r requirements/test.txt

      - save_cache:
          paths:
            - ~/venv
          key: v32-dependencies-{{ checksum "requirements/web.txt" }}-{{ checksum "requirements/test.txt" }}-{{ checksum "requirements/daemons/btc.txt" }}-{{ .Branch }}-<< parameters.v >>

jobs:
  test:
    parameters:
      v:
        type: string
        default: "3.7"

    executor:
      name: main-executor
      v: << parameters.v >>

    environment:
      PYTHON: << parameters.v >>

    working_directory: ~/repo

    steps:
      - checkout

      - install_dependencies

      - run:
          name: prepare daemon
          command: |
            . ~/venv/bin/activate
            make testnet
          background: true

      - run:
          name: run tests
          command: |
            . ~/venv/bin/activate
            make ci
            codecov -F tests -e PYTHON

  functional-tests:
    executor:
      name: main-executor
      v: "3.7"

    working_directory: ~/repo

    environment:
      BITCOIND_VERSION: 0.21.0

    steps:
      - checkout

      - run: echo $BITCOIND_VERSION > /tmp/bitcoind-version

      - restore_cache:
          keys:
            - v1-bitcoind-{{ checksum "/tmp/bitcoind-version" }}

      - install_dependencies

      - run:
          name: install regtest utils
          command: |
            . ~/venv/bin/activate
            mkdir -p ~/bitcoind && cd ~/bitcoind
            wget -N https://bitcoin.org/bin/bitcoin-core-$BITCOIND_VERSION/bitcoin-$BITCOIND_VERSION-x86_64-linux-gnu.tar.gz
            tar xzf bitcoin-$BITCOIND_VERSION-x86_64-linux-gnu.tar.gz
            sudo install -m 0755 -o root -g root -t /usr/local/bin bitcoin-$BITCOIND_VERSION/bin/*
            sudo apt install screen
            pip install electrumx

      - save_cache:
          paths:
            - ~/bitcoind
          key: v1-bitcoind-{{ checksum "/tmp/bitcoind-version" }}

      - run:
          name: prepare bitcoind
          command: |
            . ~/venv/bin/activate
            make bitcoind
          background: true

      - run:
          name: prepare electrumx
          command: |
            . ~/venv/bin/activate
            make electrumx
          background: true

      - run:
          name: prepare regtest daemon
          command: |
            . ~/venv/bin/activate
            make regtest
          background: true

      - run:
          name: run tests
          command: |
            . ~/venv/bin/activate
            make functional
            codecov -F functional-tests

  deploy:
    docker:
      - image: cimg/python:3.7
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: set up build environment
          command: |
            git clone https://github.com/bitcartcc/bitcart-docker
            cd bitcart-docker
            ./dev-setup.sh $CIRCLE_BRANCH
      - setup_remote_docker
      - run:
          name: Publish docker images
          command: |
            cd .circleci
            docker login --username=$DOCKER_USER --password=$DOCKER_PASS
            python3 build.py

workflows:
  version: 2
  build_and_test:
    jobs:
      - test:
          name: test-<< matrix.v >>
          matrix:
            parameters:
              v:
                - "3.7"
                - "3.8"
                - "3.9"
      - functional-tests:
          requires:
            - test
      - deploy:
          context: global
          requires:
            - test
            - functional-tests
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /[0-9]+(\.[0-9]+)*/
