rules:
  unpinned-uses:
    config:
      policies:
        "bitcart/*": ref-pin
  secrets-outside-env:
    config:
      allow:
        - CODECOV_TOKEN # coverage uploads is safe
  unpinned-images: # TODO: remove when enabling pinning
    ignore:
      - ci.yml
