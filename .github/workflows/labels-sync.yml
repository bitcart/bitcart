name: Sync labels

on:
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch:

permissions: {}

jobs:
  sync-labels:
    name: sync-labels
    uses: bitcart/bitcart-actions/.github/workflows/labels-sync.yml@c754f3cad8df1d5af262ed262986c5abdc1e80ed # master
    permissions:
      issues: write # to synchronize issue labels
